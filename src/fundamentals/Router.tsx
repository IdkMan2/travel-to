import React from 'react';
import {BrowserRouter, Switch, Route} from 'react-router-dom';

function Router() {

  const renderRoute = {

  };

  return (
    <BrowserRouter>
      <Switch>

      </Switch>
    </BrowserRouter>
  );
}

export default Router;
